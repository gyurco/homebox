DISTRO_NAME="homebox"
DISTRO_VERSION="0.1"

DISTRO_FEATURES = "alsa bluetooth ext2 usbgadget usbhost wifi nfs pci systemd pam ldap sasl ${DISTRO_FEATURES_LIBC}"

VIRTUAL-RUNTIME_init_manager = "systemd"

DISTRO_FEATURES_BACKFILL_CONSIDERED = "sysvinit"

LICENSE_FLAGS_WHITELIST = "commercial"

INHERIT += "rm_work"

PREFERRED_VERSION_linux-yocto = "4.1%"

PREFERRED_VERSION_samba = "4.2.3"
PREFERRED_VERSION_talloc = "2.1.2"

PREFERRED_PROVIDER_jpeg = "libjpeg-turbo"
PREFERRED_PROVIDER_jpeg-native = "libjpeg-turbo-native"

PACKAGECONFIG_pn-mpd = "mad id3tag lame"
PACKAGECONFIG_pn-systemd += "networkd"
PACKAGECONFIG_pn-openldap = "gnutls modules ldap meta monitor null passwd shell proxycache dnssrv mdb"
PACKAGECONFIG_pn-nmap = "ncat nping ndiff pcap"
